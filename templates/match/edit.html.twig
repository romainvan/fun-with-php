{% extends 'base.html.twig' %}

{% block title %}Edit Match{% endblock %}

{% block content_body %}
    <h1>Edit Match</h1>

    {{ include('match/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('match_index') }}" class="btn btn-info">back to list</a>

    {{ include('match/_delete_form.html.twig') }}
{% endblock %}

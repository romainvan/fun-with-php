{% extends 'base.html.twig' %}

{% block title %}Match{% endblock %}

{% block content_body %}
    <h1>Match</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ match.id }}</td>
            </tr>
            <tr>
                <th>ScorePlayerA</th>
                <td>{{ match.scorePlayerA }}</td>
            </tr>
            <tr>
                <th>ScorePlayerB</th>
                <td>{{ match.scorePlayerB }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ match.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('match_index') }}">back to list</a>

    <a href="{{ path('match_edit', {'id': match.id}) }}">edit</a>

    {{ include('match/_delete_form.html.twig') }}
{% endblock %}

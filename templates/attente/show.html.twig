{% extends 'base.html.twig' %}

{% block title %}Attente{% endblock %}

{% block body %}
    <h1>Attente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ attente.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ attente.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('attente_index') }}">back to list</a>

    <a href="{{ path('attente_edit', {'id': attente.id}) }}">edit</a>

    {{ include('attente/_delete_form.html.twig') }}
{% endblock %}
